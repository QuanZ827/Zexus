<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ─── Font Family resources (Step 7) ───
         Metropolis / Geist / JetBrains Mono are bundled-or-fallback. The fallback
         chain ends at fonts pre-installed on every Windows machine. -->
    <FontFamily x:Key="HeadingFont">Metropolis, Google Sans, Segoe UI Variable, Segoe UI</FontFamily>
    <FontFamily x:Key="BodyFont">Geist, Google Sans, Segoe UI Variable, Segoe UI, Segoe UI Emoji</FontFamily>
    <FontFamily x:Key="LabelFont">Geist, Google Sans, Segoe UI Variable, Segoe UI</FontFamily>
    <FontFamily x:Key="MonoFont">JetBrains Mono, Cascadia Code, Consolas, Courier New</FontFamily>

    <!-- ─── Uppercase metadata label (10px, Medium, tracking) ─── -->
    <Style x:Key="UppercaseLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontFamily" Value="{StaticResource LabelFont}"/>
        <Setter Property="Foreground" Value="{DynamicResource OutlineBrush}"/>
        <Setter Property="Typography.Capitals" Value="AllSmallCaps"/>
    </Style>

    <!-- ─── Glass surfaces ─── -->
    <Style x:Key="GlassPanelStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Background" Value="{DynamicResource GlassPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
    </Style>

    <Style x:Key="GlassFloatingStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Background" Value="{DynamicResource GlassFloatingBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderFloatingBrush}"/>
    </Style>

    <!-- ─── Gradient send button (blue → purple → pink, with neon glow) ─── -->
    <Style x:Key="StitchSendButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="SendBorder" CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            Background="{DynamicResource SendButtonGradientBrush}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="0"
                                              Opacity="0.40" Color="#A855F7"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SendBorder" Property="Background"
                                    Value="{DynamicResource SendButtonGradientHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="SendBorder" Property="Background" Value="#1AFFFFFF"/>
                            <Setter TargetName="SendBorder" Property="Effect" Value="{x:Null}"/>
                            <Setter Property="Foreground" Value="#4DFFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ─── Scrollbar (thin, glass) ─── -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="5"/>
        <Setter Property="MinWidth" Value="5"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0">
                                    <RepeatButton.Template>
                                        <ControlTemplate><Border Background="Transparent"/></ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#30FFFFFF" CornerRadius="2.5"
                                                    Margin="1,0" Opacity="0.4"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0">
                                    <RepeatButton.Template>
                                        <ControlTemplate><Border Background="Transparent"/></ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="5"/>
                <Setter Property="MinHeight" Value="5"/>
                <Setter Property="Width" Value="Auto"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
